/**
 * Parsed-reports guide inventory UI + tool payload version. Bump when schema, copy, or
 * layout changes. Must stay a compile-time string — do not read from process.env.
 */
export const PEARLMANAI_GUIDE_UI_VERSION = "1.0.0" as const;
